language: ruby
rvm:
  - 2.1.0
  - jruby
  - rbx
env:
  - DB=postgresql

script:
  - bundle exec rake db:create
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database delta_bd' -U postgres